<!--
Template. In the target repository, copy this file to `.github/copilot-instructions.md`.

Copilot modes do not all have the same repository write capabilities. Verify whether the active mode can create branches, commits, and pull requests. If it cannot, follow the protocol's read-only fallback.
-->

# Persistent memory — GitHub Copilot

Before starting any work in this repository, read `repodoc/memory-protocol.md` and follow its persistent memory protocol for the entire session.

Read it again if it has changed since the previous session. It describes the current protocol and must not be assumed from memory.
