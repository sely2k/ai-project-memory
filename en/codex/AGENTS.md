<!--
Template. In the target repository, copy this file to `AGENTS.md` at the repository root.

Unlike CLAUDE.md, AGENTS.md has no `@path` import mechanism guaranteed across every tool that reads it. The instruction below therefore tells the agent to read the protocol explicitly.
-->

# Persistent memory — Codex

Before starting any work in this repository, read `repodoc/memory-protocol.md` and follow its persistent memory protocol for the entire session.

Read it again if it has changed since the previous session. It describes the current protocol and must not be assumed from memory.
