<!--
Bozza. Nel repository target, il contenuto di questo file va copiato in
`.github/copilot-instructions.md` alla radice del repository (accanto a `repodoc/memory-protocol.md`).

Nota: Copilot Chat/Coding Agent legge questo file come contesto di generazione, ma non tutte le
modalità di Copilot dispongono di accesso in scrittura al repository (creare branch, commit, PR)
allo stesso modo di Claude Code o Codex. Verificare quale modalità è in uso (Copilot Chat in IDE
vs. Copilot coding agent) prima di assumere che il flusso di PR persistente sia eseguibile
autonomamente: in assenza di scrittura, il protocollo stesso prevede di proporre il contenuto
invece di scriverlo (vedi sezione "Pull Request persistente" in memory-protocol.md).
-->

# Memoria persistente — GitHub Copilot

Prima di iniziare qualsiasi lavoro su questo repository, leggi `repodoc/memory-protocol.md` e segui il protocollo di memoria persistente che descrive per l'intera sessione.

Rileggilo se il file risulta cambiato rispetto all'ultima sessione: descrive lo stato corrente del protocollo, non va assunto da memoria.
