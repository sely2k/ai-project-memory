<!--
Bozza. Nel repository target, il contenuto di questo file va copiato in `AGENTS.md`
alla radice del repository (accanto a `repodoc/memory-protocol.md`), non lasciato in questa sottocartella.

Nota: a differenza di CLAUDE.md, AGENTS.md non ha un meccanismo di import standard (`@path`)
garantito da tutti gli strumenti che lo leggono. Il pointer qui sotto è quindi un'istruzione
testuale che l'agente deve eseguire attivamente (leggere il file), non un'inclusione automatica
a caricamento. Verificare che lo strumento in uso segua effettivamente l'istruzione.
-->

# Memoria persistente — Codex

Prima di iniziare qualsiasi lavoro su questo repository, leggi `repodoc/memory-protocol.md` e segui il protocollo di memoria persistente che descrive per l'intera sessione.

Rileggilo se il file risulta cambiato rispetto all'ultima sessione: descrive lo stato corrente del protocollo, non va assunto da memoria.
